# --- Python ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/

# --- dbt ---
target/
dbt_packages/
logs/
.user.yml
profiles.yml.user

# --- Generated data & DuckDB databases ---
# The bags and every warehouse layer are regenerable from a seed, so they are
# never committed. `make build` recreates all of it from scratch.
data/raw/
data/bronze/
data/silver/
data/gold/
*.duckdb
*.duckdb.wal
*.db

# --- `make verify` scratch output ---
.verify/

# --- OS ---
.DS_Store
